import { createRenderProfileSelectOptions } from './team-profile-select-options-render-action.js';

export function createRenderProfileSelectOptionsAction(deps) {
    return createRenderProfileSelectOptions(deps);
}
