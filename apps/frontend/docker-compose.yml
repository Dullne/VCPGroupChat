# Standalone frontend debugging only. The default product stack is the
# repository-root compose file, where GroupChatBackend serves this frontend
# from the same `groupchat-app` container on port 7010.
services:
  llmgroupchat:
    image: nginx:alpine
    container_name: llmgroupchat
    ports:
      - "4090:80"
    volumes:
      - .:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
